export default class ParticleTexture {
    constructor(textureId, numOfRows, additive) {
        this.textureId = textureId;
        this.numOfRows = numOfRows;
        this.additive = additive;
    }
}